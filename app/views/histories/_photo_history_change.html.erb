<li>
  <div>
	<%= datetime %>
	<% unless new_photos.empty? %>
          <%= new_photos.size > 1 ? "Photos" : "Photo" %>
          <% new_photos.each do |new_photo| %>
            <%= link_to_photo_with_key(new_photo) %> 
          <% end %>
          added by <%= user_name %>
	<%end%>
  </div>
</li>

