<% if current_user_name != user.user_name %>
  <input type="checkbox" class="user-disabled-status"
    <%= (user.disabled=="true") ? "checked='checked'" : "" %>
  />
<% else %>
  <%= h "Enabled" %>
<% end %>
