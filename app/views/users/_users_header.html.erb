<tr>
  <th>Full Name</th>
  <th><%= t("login.username") %></th>
  <% if can?(:disable, User) %>
    <th>Disabled?</th>
  <% end %>
  <% if can? :show, User %>
    <th></th>
  <% end %>
  <% if can? :edit, User %>
      <th></th>
  <% end %>
  <% if can? :destroy, User %>
      <th></th>
  <% end %>
</tr>
