
<%= label_tag select_box.tag_name_attribute, select_box.name.humanize %>
<%= select_tag select_box.tag_name_attribute, options_for_select(select_box.options.collect { |option| [option.option_name, option.option_name] } << ["", ""], "") %>
