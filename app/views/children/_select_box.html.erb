<% name = field["name"] %>
<% select_tag_name = "child[#{form_name}][#{name}]" %>
<% options = field["options"] %>


<%= label_tag select_tag_name, name.humanize %>
<%= select_tag select_tag_name, options_for_select(options.collect { |option| [option, option] } << ["", ""], "") %>
