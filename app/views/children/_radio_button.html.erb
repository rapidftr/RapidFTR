
<%= label_tag radio_button.tag_name_attribute, radio_button.name.humanize %><br/>

<% radio_button.options.each do |option| %>
    <%= radio_button_tag radio_button.tag_name_attribute, option.option_name.capitalize %>
    <%= label_tag option.tag_name_attribute, option.option_name.capitalize %>
<% end %>

