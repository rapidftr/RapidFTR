<p>
  <span class="tool-tip-label">
	<%= label_tag check_boxes.tag_name_attribute, check_boxes.display_name, {:class=>"main-label"}%>
    <% if check_boxes.help_text.present? %>
	    <img class="tool-tip-icon vtip" title="<%=check_boxes.help_text%>" src="/images/tool_tip_icon.gif"/>
    <% end%>
  </span>
    <span class="checkbox">
	    <% check_boxes.options.each do |option| %>
				<%= check_box_tag check_boxes.tag_name_attribute+"[]", option.option_name, (@child[check_boxes.name]||[]).include?(option.option_name), :id => option.tag_id  %>
				<%= label_tag option.tag_id, option.option_name %>
		<% end %>
    </span>
</p>
