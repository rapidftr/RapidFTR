<% form_for :field, @field, :url => { :action => "create" } do |f| %>
        <p>Name</p>
      <%= f.text_field :name %>
    <p>Help text</p>

      <%= f.text_area :help_text %>
      <%= submit_tag 'Create' %>
<% end %>