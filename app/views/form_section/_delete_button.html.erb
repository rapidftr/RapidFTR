<% form_id = "#{field.name}_DeleteForm" %>
<% form_for field, :url => url, :html => {:method => :post, :id=>form_id}  do |f| %>
	<%= hidden_field_tag "field_name", "#{field.name}"%>
	<%= submit_tag "Delete", {:id => "#{field.name}_Delete", :class => "delete-button", :confirm => "#{warning}" } %>
<% end %>